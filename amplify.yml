version: 1
frontend:
  phases:
    build:
      commands:
        - |
          if [ "${AWS_BRANCH}" != "main" ]; then
            printf 'User-agent: *\nDisallow: /\n' > robots.txt
          fi
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
